# Attack Engine dependencies

# Core / shared
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0
tenacity>=8.2,<9.0
langgraph>=0.1.5

# Attack Planner
neo4j>=5.20,<6.0

# Prompt Generator
jinja2>=3.1,<4.0

# Attack Executor (provider adapters - OpenAI/Anthropic/Gemini/Ollama are
# plain REST calls via httpx, no vendor SDKs required)
httpx>=0.27,<1.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23

# --- Optional extras (install only if you use the corresponding provider) ---
# huggingface_local provider:
#   pip install transformers torch
